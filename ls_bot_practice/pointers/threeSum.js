/**
 * Given an array of integers, find all unique triplets in the array which give the sum of zero.
 *
 * Problem:
 *  - The input is an array of integers
 *  - The output is an array of all valid triplets
 *  - A valid triplet is three separate elements from nums that add up to zero
 *  - The integers can be either positive or negative
 *  - The triplets don't have to be contiguous
 *
 * Algorithm:
 *
 */

function threeSum(nums) {
  // code here
}

// Example usage:
console.log(threeSum([-1, 0, 1, 2, -1, -4])); // -4, -1, -1, 0, 1, 2
// Should return [[-1, -1, 2], [-1, 0, 1]]
